## v20260110215126 (2026-01-10 21:51:26)
**Snapshot Release**

- **Feature**: `gTTS.py` — wrapper that pipes `gtts-cli` output to `ffplay` for immediate playback.
- **Tooling**: Batch (`.bat`) and PowerShell (`.ps1`) helper scripts are included; several live in the `a/` folder as archived/legacy utilities for reference.
- **Note**: Update absolute paths in `gTTS.py` or use system-installed `gtts-cli`/`ffplay` on PATH. Use `a/` scripts only for historical reference.

See `ref/release-notes.md` for examples of the changelog format.
