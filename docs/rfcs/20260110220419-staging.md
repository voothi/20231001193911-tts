20260110220419

# RFC: Release v1.0.0 Staging

ZID: 20260110220419

## Summary / Request
This RFC stages the release identified by ZID above and records the user's request to "Release v1.0.0" along with documentation updates and decisions made during preparation.

## Staging
- Prepare project documentation updates (`README.md`, `release-notes.md`).
- Record RFC in `docs/rfcs/` starting with the ZID.
- Mark legacy `a/` folder as archived in docs; do not remove files without explicit permission.
- Tag repository with `v1.0.0` (action to be performed by repository owner after review).

## Implementation Details

- Analytics: inspected repository contents; primary scripts are `gTTS.py` (root) and several legacy helpers under `a/`.
- Decisions:
  - Keep archived helpers in `a/` for reference rather than deleting; update docs to state `a/` is archived to avoid confusion.
  - Add recommended `venv/` setup instructions for Windows, Linux, and macOS to improve onboarding.
  - Add `Kardenwort` section to indicate project membership in the personal utility ecosystem.
  - Create Release Notes entry `v1.0.0` with timestamp `2026-01-10 22:04:19` (ZID) summarizing included features and docs changes.
  - Add a version badge to `README.md` that links to the `release-notes.md` entry for quick reference.

## Files Added / Modified
- Added: `docs/rfcs/20260110220419-staging.md` (this file)
- Modified: `README.md` (version badge, docs links, venv instructions already added)
- Modified: `release-notes.md` (added `v1.0.0` entry)

## Notes
- This RFC is an internal staging/record artifact. Tagging and publishing the release should be done by the repository owner after they confirm the changes.
